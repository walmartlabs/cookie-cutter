{
    "amqp": {
        "server": {
            "host": "CREATE local.json with same structure as default.json but put real info in there like 'localhost'"
        },
        "queue": {
            "name": "defaultQueueName"
        }
    }
}